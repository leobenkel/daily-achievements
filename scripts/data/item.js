define([
    'scripts/data/database.js'
],
    function (db) {
        /*
            * item_id (PK)
            * note_id (required)
            * tag_id (required)
            * content (can be empty and will display: Worked on [TAG]
        */
    });
